{
  "name": "@jameet/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Two-person remote music sessions",
  "author": "JaMeet",
  "desktopName": "JaMeet",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "dev:2": "node scripts/dev-instance.cjs 2",
    "dev:instance": "node scripts/dev-instance.cjs",
    "build:native": "node scripts/build-native.cjs",
    "build": "npm run build:native && electron-vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "package:mac": "npm run build && electron-builder --mac --publish never",
    "package:mac:pkg": "npm run build && node scripts/build-macos-pkg.cjs",
    "package:mac:arm64": "npm run build && electron-builder --mac dmg --arm64 --publish never",
    "package:win": "npm run build && electron-builder --win --x64 --publish never",
    "package:win:x64": "npm run build && electron-builder --win --x64 --publish never",
    "package:linux": "npm run build && electron-builder --linux --publish never"
  },
  "dependencies": {
    "@jameet/shared": "0.1.0",
    "lucide": "^1.31.0",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "electron": "43.4.0",
    "electron-builder": "^26.0.12",
    "electron-vite": "^5.0.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^4.1.10"
  },
  "build": {
    "appId": "com.jameet.app",
    "productName": "JaMeet",
    "executableName": "JaMeet",
    "asar": true,
    "files": [
      "out/**/*",
      "package.json"
    ],
    "protocols": [
      {
        "name": "JaMeet Session",
        "schemes": [
          "jameet",
          "musiczoom"
        ]
      }
    ],
    "mac": {
      "extraResources": [
        {
          "from": "bin",
          "to": "bin",
          "filter": [
            "!**/*.driver",
            "!**/*.driver/**"
          ]
        }
      ],
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "category": "public.app-category.music",
      "hardenedRuntime": true,
      "extendInfo": {
        "NSCameraUsageDescription": "JaMeet uses your camera for remote music sessions.",
        "NSMicrophoneUsageDescription": "JaMeet uses your microphone or audio interface for remote music sessions."
      }
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "JaMeet"
    },
    "linux": {
      "target": [
        {
          "target": "AppImage",
          "arch": [
            "x64"
          ]
        }
      ],
      "category": "AudioVideo",
      "syncDesktopName": true
    },
    "directories": {
      "output": "release"
    },
    "artifactName": "JaMeet-${version}-${os}-${arch}.${ext}"
  }
}
